export interface Settings {
  defaultLat: number;
  defaultLon: number;
  defaultZoom: number;
  googleMaps: Object;
  heatmapOptions: google.maps.visualization.HeatmapLayerOptions;
  imagePath: Object;
  maxZoom: number;
}
